apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: devops-backend
  namespace: backend
spec:
  chart:
    spec:
      version: v0.0.7
  values:
    image:
      tag: v0.0.7
    replicaCount: 2
